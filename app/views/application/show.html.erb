<%= provide :h1, content_tag(:h1, instance_variable_get("@#{controller_name.singularize}").content_header, class: 'h1 h1-page bg-fff') %>

<%= render(@templates.blank? ? 'no_content' : @templates) %>
